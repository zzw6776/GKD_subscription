# 项目规则

## 新增规则的默认状态

- 以后新增的规则和规则组必须默认启用，除非用户明确要求默认关闭。
- 新增规则组时不能只设置 `enable: true`。GKD 的分类默认值会优先于规则组的 `enable`，因此还必须确认规则组没有被默认关闭的分类覆盖。
- 不得为了启用一条新规则而打开包含其他规则的整个默认关闭分类。应使用默认启用的专用分类，或避免将规则组归入默认关闭的分类。
- 修改后必须同时检查订阅源数据和 GKD 的实际规则解析优先级，确认新规则最终为启用状态。
- 新增单条规则时不得改动同组已有规则的选择器、顺序或动作，除非用户明确授权。
